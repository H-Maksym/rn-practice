import { StyleSheet, Dimensions } from "react-native";
import { theme } from "src/utils/theme";
export const stylesMapScreen = StyleSheet.create({
  headerIconGoBack: {
    marginLeft: 16,
    color: "#21212180",
  },
});
